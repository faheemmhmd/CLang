/*Take a number(n) from user & output its cube(n*n*n).*/
#include <stdio.h>

int main(){
    int n;
    printf("Enter the no. : ");
    scanf("%d",&n);
    printf("The cube of the no. is : %d \n",n*n*n);
    return 0;

}